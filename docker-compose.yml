services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: byld
      POSTGRES_USER: byld
      POSTGRES_PASSWORD: byld_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  postgres_data:
